# ai-note

## Git

Solo notes repo — commit and push directly to `main`. Never open PRs or delete remote branches
unless I ask. (`gh pr create` is hook-enforced here.)

## Markdown tables

**Settled — don't re-run formatting experiments.** Prettier version is NOT the variable: 2.8.8 and
3.x produce identical output on CJK content.

Decision still pending, and it's mine to make:

- accept CJK-width-2 padding, or
- exclude tables from formatting

Until I pick one, leave table formatting alone.
